# A12Bypass
Open Source A12+ Bypass

Prerequisites
Client-Side (macOS/Linux)
Python 3.6+
libimobiledevice (via Homebrew on macOS)
pymobiledevice3 (via pip)
curl
Server-Side
PHP 7.4 or newer
SQLite3 extension enabled
Write permissions for cache directories
